import { CountAnimationDirective } from './count-animation.directive';

describe('CountAnimationDirective', () => {
  it('should create an instance', () => {
    const directive = new CountAnimationDirective(null);
    expect(directive).toBeTruthy();
  });
});
